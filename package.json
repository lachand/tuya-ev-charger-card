{
  "name": "tuya-ev-charger-card",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && esbuild src/tuya-ev-charger-card.ts --bundle --format=esm --target=es2020 --outfile=tuya-ev-charger-card.js && cp tuya-ev-charger-card.js dist/tuya-ev-charger-card.js",
    "watch": "esbuild src/tuya-ev-charger-card.ts --bundle --format=esm --target=es2020 --outfile=tuya-ev-charger-card.js --watch"
  },
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.1",
    "typescript": "^5.8.2"
  }
}
